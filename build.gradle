plugins {
    id 'eclipse'
    id 'maven-publish'
    id 'net.minecraftforge.gradle' version '6.0.+'
}

group = 'com.wira.finalprotocol'
version = '1.0.0'
archivesBaseName = 'FinalProtocol'

java.toolchain.languageVersion = JavaLanguageVersion.of(17)

minecraft {
    mappings channel: 'official', version: '1.20.1'

    runs {
        client {
            workingDirectory project.file('run')
            property 'forge.logging.console.level', 'debug'
            mods {
                finalprotocol {
                    source sourceSets.main
                }
            }
        }
        server {
            workingDirectory project.file('run')
            property 'forge.logging.console.level', 'debug'
            mods {
                finalprotocol {
                    source sourceSets.main
                }
            }
        }
    }
}

repositories {
    maven {
        name = "CurseMaven"
        url = "https://cursemaven.com"
    }
    mavenCentral()
    maven { url = "https://maven.minecraftforge.net" }
}

dependencies {
    minecraft 'net.minecraftforge:forge:1.20.1-47.4.10'

    // Mekanism dependency
    implementation fg.deobf("curse.maven:mekanism-268560:5798032") // Mekanism 10.4.0+

    // Optional: JEI for dev
    runtimeOnly fg.deobf("curse.maven:jei-238222:5798080")
}

jar {
    manifest {
        attributes([
                "Specification-Title": "Final Protocol",
                "Specification-Vendor": "WQ",
                "Specification-Version": "1",
                "Implementation-Title": project.name,
                "Implementation-Version": project.version,
                "Implementation-Vendor": "Bohdan Ihnatenko (WQ)",
                "Implementation-Timestamp": new Date().format("yyyy-MM-dd'T'HH:mm:ssZ")
        ])
    }
}

tasks.withType(JavaCompile).configureEach {
    options.encoding = 'UTF-8'
}